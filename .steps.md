# Tools

## Pipx

```bash
py -m pip install --user pipx
cd ~\AppData\Roaming\Python\Python313\Scripts
.\pipx.exe ensurepath
```

Restart the terminal 

```bash
pipx --version
pipx completions
```

## Poetry

```bash
pipx install poetry 
poetry --version
```

## Build

```bash
pipx install build
pyproject-build --version
```

## Tox

```bash
pipx install tox
tox --version
```

## Pre-Commit

```bash
pipx install pre-commit
pre-commit --version
```

## Cookiecutter

```bash
pipx install cookiecutter
cookiecutter --version
```


```bash
pyproject-build # will raise an error
touch pyproject.toml
pyproject-build 